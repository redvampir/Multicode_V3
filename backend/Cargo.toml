[package]
name = "backend"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
tauri = { version = "1" }
serde_json = "1"

[build-dependencies]
tauri-build = { version = "1" }
